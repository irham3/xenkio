// Gradient Generator Feature - Public API
export { GradientGenerator } from './components/gradient-generator';
export { useGradientGenerator } from './hooks/use-gradient-generator';

// Types
export type { GradientConfig, ColorStop, GradientType, GradientPreset } from './types';

// Constants
export { DEFAULT_GRADIENT, GRADIENT_PRESETS, MIN_STOPS, MAX_STOPS } from './constants';
